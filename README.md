# M3S08-Exercicio07
instalar dependencias do front e executar
```cmd
  cd front
  npm install
  npm start
```
instalar dependencias do server e executar
```cmd
  cd server
  npm install
  node server.js
```
